envAlias: dev

visitor-management:
  ingress:
    enabled: true
    #ingressClassName: iap-nginx
    service:
      name: vms-dev-visitor-management
      port:
        name: http

    hosts:
      - temp.internal.napbiotec.io # will change to vms-dev-visitor-management
